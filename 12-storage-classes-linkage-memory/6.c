/* Calculates 1000 random numbers (1-10) and stores occurances of each
 * Prints occurences on line:
 * Position is random number, value is occurences
 * Do this X 10 lines. 
*/

#include "6.run.h"

int main(void){
	for(int i=0; i < 10; i++)
		run(1000);
	return 0;
}
